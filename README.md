This newmaster branch is because I attempted the following which turned into a mess
    checkout master
    git pull upstream master
    # then fix conflicts and git rebase --continue to end.

So I backed up 1 commit for master and named it newmaster.
Seems like my cheat is hopelessly conflicted with upstream and need a day to fix it


